# Write a function that validates a URL string. Handle the ValueError by raising a custom
# exception if the URL format is invalid.

def url_isvalid(input_string: str) -> bool:
    ans = True
    if input_string not
    return ans


print(url_isvalid("https://www.google.com/"))
