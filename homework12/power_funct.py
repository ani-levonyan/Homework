# Write a function that takes a base and an exponent and returns the result of raising the
# base to the exponent.

def power_funct(base, exponent):
    return base ** exponent


print(power_funct(5, -2))
